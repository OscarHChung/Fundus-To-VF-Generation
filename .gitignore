encoder/RETFound_cfp_weights.pth
data/fundus/grape_fundus_images/*.jpg
data/*.pt
decoder/*.pth
encoder/*.pth

# PAPILA raw download (zip + extracted images/spreadsheets) -- see data/external/papila/SOURCE.md
# Only the harmonized data/external/papila_records.json + SOURCE.md are tracked.
data/external/papila/*
!data/external/papila/SOURCE.md

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Generated checkpoint binaries inside results
decoder/results/**/*.pth

# Task 3 (P-A2): PAPILA frozen-feature cache -- reproducible via
# `python decoder/diag_papila_severity.py --cache`; not committed.
decoder/results/auto/papila_sev_features.npz

# Task 4 (P-A1/A3): RetiZero / RETFound-Green frozen-feature caches -- reproducible via
# `python decoder/diag_encoder_bakeoff.py --cache --only retizero` and the dedicated
# RETFound-Green caching script (see .superpowers/sdd/task-4-report.md); not committed.
decoder/results/auto/bakeoff_retizero.npz
decoder/results/auto/bakeoff_retfound_green__full392.npz
